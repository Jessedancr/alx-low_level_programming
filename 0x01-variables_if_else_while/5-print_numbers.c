#include <stdio.h>
/**
 * main - print if the number is positive negative or zero
 * Description: using the main functon
 * this program prints blah blah blah
 * Return: 0
 */
int main(void)
{
	int x = 0;

	for (x = 0; x <= 9; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
